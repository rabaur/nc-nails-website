# SASS cache
.sass-cache/
*.css.map

# Node modules (if ever used)
node_modules/

# Compiled CSS (keep only SCSS in repo, ignore compiled CSS if desired)
styles/main.css

# OS files
.DS_Store

# Log files
*.log 